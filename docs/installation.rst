============
Installation
============

At the command line::

    $ easy_install wagtailcrm

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wagtailcrm
    $ pip install wagtailcrm
